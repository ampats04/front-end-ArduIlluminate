import 'package:flutter/material.dart';

class PowerMeterPage extends StatelessWidget {
  const PowerMeterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is the Energy screen'),
    );
  }
}
